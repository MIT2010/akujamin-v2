name: feature_history
description: >-
  Test history + certificate view — migrated from the old app's "Riwayat"
  dashboard tab (`payment`'s `list-voucher` endpoint, feature #5, deliberately
  the smallest safe slice of the payment/test/counseling/websocket cluster:
  read-only, no camera, no websocket, no write-path). See MIGRATION_LOG.md.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  core:
    path: ../core
  design_system:
    path: ../design_system
  shared:
    path: ../shared
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.1.0
  go_router: ^17.3.0
  injectable: ^3.0.0
  intl: ^0.20.2
  json_annotation: ^4.12.0
  pdfx: ^2.9.1

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  injectable_generator: ^3.0.0
  json_serializable: ^6.14.0
  mocktail: ^1.0.4

flutter:
