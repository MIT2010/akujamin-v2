/// The `/home` shell tab (§5): profile teaser + incomplete-profile nudge,
/// migrated from the old app's real dashboard home screen — see
/// `home_page.dart`'s doc comment for what this replaced and why.
library;

export 'src/presentation/pages/home_page.dart';
